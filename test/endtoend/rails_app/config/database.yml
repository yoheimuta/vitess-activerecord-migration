default: &default
  adapter: mysql2
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  host: migration-mysql
  username: root
  database: main

test:
  <<: *default
  # host: migration-endtoend
  # username: user
  host: migration-mysql
  username: root
  database: main-test
